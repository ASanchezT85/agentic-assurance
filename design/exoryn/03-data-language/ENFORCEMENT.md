# Enforcement

A recommendation is visually non-binding: outline/neutral treatment and explicit `Recommended only`. A customer-authorized control is visually binding: solid state marker plus `Enforcing`. Revoked/expired controls remain historical and subdued. The read-only Console contains no control-authoring buttons.
