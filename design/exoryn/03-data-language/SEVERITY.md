# Incident Severity

LOW, MEDIUM, HIGH, CRITICAL are categorical labels. Severity is shown with text + side marker + icon.

Critical/high may use danger hues; medium uses warning; low uses information blue. Do not animate severity.
